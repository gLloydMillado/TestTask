server {
    listen 80;
    server_name pma.test;

    location / {
        proxy_pass http://laravel_pma:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}